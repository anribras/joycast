# xiangting project

## crawl rss feeds source
```sh
cd crawler
scrapy crawl xima_cates -O jsons/ximalaya_categories.json -t jsonlines
scrapy crawl xima_cates -O jsons/ximalaya_categories.json -t jsonlines
```
